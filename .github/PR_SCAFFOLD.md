## Pull request scaffold

This file is a marker created so the PR (`claude/app-review-findings-55ErI` -> `main`) has a
distinct base commit and can be opened. It will be removed once the review work is merged.
